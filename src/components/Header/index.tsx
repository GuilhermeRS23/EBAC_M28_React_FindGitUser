import styles from './Header.module.css';

const Header = () => {
    return (
        <header className={styles.title}>
            <h1>FindUser GitHub</h1>
        </header>
    )
}

export default Header;
